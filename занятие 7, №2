import random as rand

n = int(input('n = '))

if n > 0:
    cat = [-5 + 26 * rand.random() for i in range(n)]
    print(cat)
    sr = sum(cat) / len(cat)
    for k, x in enumerate(cat):
        if x == 0:
            cat[k] = sr
    print(cat)
